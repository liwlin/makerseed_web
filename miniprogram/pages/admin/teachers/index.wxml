<view class="page">
  <view class="section-title">教师管理</view>
  <view class="list-card" wx:for="{{teachers}}" wx:key="name">
    <view>
      <view class="item-title">{{item.name}}</view>
      <text>{{item.role}}</text>
    </view>
  </view>
</view>
