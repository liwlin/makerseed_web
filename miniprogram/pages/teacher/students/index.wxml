<view class="page">
  <view class="section-title">我的学员</view>
  <view class="list-card" wx:for="{{students}}" wx:key="_id">
    <view>
      <view class="item-title">{{item.name}}</view>
      <text>{{item.grade}} · {{item.course}}</text>
    </view>
  </view>
</view>
