window.MAKERSEED_CONFIG = {
  // Optional production endpoint for website bookings.
  // Leave empty to use browser localStorage only.
  bookingEndpoint: "",

  // Optional request headers for a form service or your own API.
  bookingHeaders: {
    "Content-Type": "application/json"
  },

  contact: {
    wechatOfficialAccount: "种子创客工坊",
    city: "广东江门",
    phone: "",
    address: "广东江门，详细地址请预约后确认"
  }
};
